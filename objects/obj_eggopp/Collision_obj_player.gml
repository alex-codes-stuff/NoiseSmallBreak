/// @description Insert description here
// You can write your code in this editor
if state == 0 && (other.movespeed >= 12.1 || ( other.state = states.slide || other.state = states.bounce))
    instance_destroy()
if state == 1 && other.state = states.slide || other.state = states.bounce
    instance_destroy()
if state == 1 && !( other.state = states.slide || other.state = states.bounce)
   obj_player.scr_hurtplayer() 



